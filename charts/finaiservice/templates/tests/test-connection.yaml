apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "finaiservice.fullname" . }}-test-connection"
  labels:
    {{- include "finaiservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "finaiservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
