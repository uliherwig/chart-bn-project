apiVersion: v1
kind: Service
metadata:
  name: {{ include "finaiservice.fullname" . }}
  labels:
    {{- include "finaiservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finaiservice.selectorLabels" . | nindent 4 }}
