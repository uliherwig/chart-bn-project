apiVersion: v2
name: bn-project
description: BN Project Helm Charts
type: application

# Helm chart version
version: 1.0.0

# BN Project version
appVersion: "0.0.1"

dependencies:
  - name: keycloak
    version: 24.4.9
    repository: https://charts.bitnami.com/bitnami

